env: dev
service: gateway

server:
  host: 0.0.0.0
  port: 8080

redis:
  addr: localhost:6379
  db: 0

nats:
  url: nats://localhost:4222

postgres:
  dsn: postgres://switchboard:switchboard@localhost:5432/switchboard?sslmode=disable
  max_conns: 10
  min_conns: 2
  max_idle_secs: 300